# PM'in İU'dan Talebi

## test_pm_iu_21072026 — Fiziksel Test Bulguları (PM → IU → Yayın kolu)

*21.07.2026. Kaynak: İskender'in fiziksel test turu (`hb_testler_pm_iu_talep_210726.xlsx`). Kapsam: üretici rol (PM) girişinden, IU üzerinden yayına almaya kadar olan kol. Etiketler: HATA (yanlış çalışan), İYİLEŞTİRME (çalışıyor ama düzeltilecek), YENİ İSTEK, OLUMLU. İş bölümü dosya/ekran bazlı paketlerledir (P1..P6); bir paket = bir commit, tüm commit'ler işin sonunda toptan.*

## P1 — Senaryo ekranı (F-4, F-5) — ÖNCELİK 1

- **F-4 | HATA:** IU, gönderdiği revize senaryoyu kendi Senaryolar ekranında açınca işaretli (üstü çizili/kırmızı) metni değil düz metni görüyor. İstenen: IU da PM gibi, onay gelene kadar değişiklikleri işaretli görebilmeli.
  - Teknik: `app/senaryolar/[talep_id]/page.tsx` içindeki `diffGoster` bilinçli olarak yalnız PM'e açılmıştı (`isPM && son_durum === "inceleme bekleniyor"`). Test bu kararı geçersiz kıldı — koşul role bakmadan `son_durum === "inceleme bekleniyor" && oncekiSenaryo` olacak.
- **F-5 | HATA:** PM onayladıktan sonra biçimler (çizgi/kırmızı) kalktı ama üstü çizili (silinmiş) ifadeler yeni ifadelerle BİRLİKTE metinde kaldı. Onay sonrası üstü çizilenler tamamen silinmeli; yalnız temiz nihai metin kalmalı.
  - Teknik: DB'ye yazılan `senaryo_metni`'nin silinen karakterleri de içerdiği anlaşılıyor — editörün temiz metin üretimi (`temizMetin`, `lib/utils/senaryo/duzeltmeModeli.ts`) ile sayfaya bağlantısı (`onDegisti` → `senaryoMetni` → gönderim) zincirinde kaçak var. Önce kanıt: hatalı kaydın DB'deki metni incelenip kaçağın yeri bulunacak; düzeltme + gerekirse hatalı kayıt için temizlik SQL'i (İskender çalıştırır).

## P2 — Soru seti sayfası (F-8, F-9, F-10) — ÖNCELİK 2

- **F-10 | HATA:** Word dosyası yüklenince tüm sorular/seçenekler/doğru şık, tablodaki 1. sorunun hücresine yığıldı ("1. soruda eksik alanlar var — formda tamamlayın" uyarısı). Beklenti: hangi formatta dosya yüklenirse yüklensin içerik, soru-seçenek tablosuna doğru dağıtılmalı — text alanına kopyala-yapıştır nasıl doğru dağıtıyorsa (o yol OLUMLU: satırlara dağıttı, doğru şıkkı otomatik tikledi) dosya yolu da aynı ayrıştırıcıdan geçmeli.
  - Teknik: `app/soru-setleri/[video_durum_id]/page.tsx` — dosya yükleme yolunun çıktısı, yapıştırma yolunun kullandığı ayrıştırıcıya bağlanacak (tek ayrıştırıcı, iki giriş).
- **F-8 | İYİLEŞTİRME:** Serbest text alanı bayat — tamamen kalkacak. Yapılandırılmış soru-seçenek tablosu tek giriş yöntemi.
- **F-9 | İYİLEŞTİRME:** "Dosya getir" buton adı → "Dosya Yükleyin". "Excel şablonu indir" linki amaçsız → kaldırılacak. Sayfada yalnız iki öğe kalacak: geliştirilmiş soru-seçenek tablosu + Dosya Yükleme butonu.

## P3 — Video ekranı (F-6, F-7) — ÖNCELİK 3

- **F-6 | HATA:** Video yüklendikten sonra kapak görseli yok ve video oynamıyor; 1-2 dakika sonra MANUEL refresh ile geldi (hem IU hem PM). Kullanıcı refresh yapmadan hazır hale gelmeli.
  - Teknik: `hooks/useBunnyIslemeDurumu.ts` sınırlı süreli tekrar-sorgu için kurulmuştu — canlıda beklendiği gibi çalışmadığı gözlendi; süre/koşul/yenileme davranışı incelenecek.
- **F-7 | İYİLEŞTİRME:** IU sayfasında "Durum" sütunundaki badge metni uzun olunca liste sütun başlıkları kayıyor — badge sabit genişlik/kısaltma ya da sütun düzeni düzeltilecek.

## P4 — Talep listesi (F-1, F-2)

- **F-1 | İYİLEŞTİRME:** Talep gönderildikten sonra PM'in taleplerim listesinde "Dosya ekle" butonu hâlâ aktif — bu aşamada dosya eklenememeli (buton pasif/gizli).
- **F-2 | İYİLEŞTİRME:** Listedeki "Senaryolar" butonu senaryo gelmeden aktif ve boş akış açıyor; karar: buton listeden TAMAMEN kaldırılacak — PM senaryo takibini Navbar'daki Senaryolar sekmesi + badge ile yapar.

## P5 — Pill/rozet renkleri (F-11)

- **F-11 | İYİLEŞTİRME:** PM soru setini onayladıktan sonra: Soru Setleri pill'i bordo kalıyor (eski rengine dönmeli); Yayın Yönetimi pill'i bordo olmalı (şu an renk değişmiyor, yalnız badge "1"). Pill renk mantığı "sıradaki iş" kuralına göre düzeltilecek.

## P6 — "Onaylanan Talepler" sekmesi (F-12) — YENİ İSTEK

- **F-12 | YENİ İSTEK:** IU'da "Talepler" gibi bir "Onaylanan Talepler" sekmesi: tek sayfada, yukarıdan aşağıya her talebin onaylı senaryosu + videosu (oynatılabilir) + soru seti, salt-okuma.

## Olumlu gözlemler

- **F-3:** Revizyon editörü istendiği gibi: backspace üstü çizdi, yeni ifade kırmızı bantta, gönderim sorunsuz.
- **F-13:** Yayın: puanlar girildi, yayına alındı, UTT ekranında gördü — sorunsuz.
- Text alanına kopyala-yapıştır ile toplu soru girişi doğru ayrıştırıldı (F-10'un referans davranışı).

## Yapılan Geliştirmeler (21.07.2026 — tümü tek commit: `Test_210726`)

- **F-4 (P1):** İşaretli görünüm onay gelene kadar herkese açıldı — `diffGoster` rol şartı kaldırıldı (`app/senaryolar/[talep_id]/page.tsx`). IU da gönderdiği revizyonun çizili/kırmızı halini görür.
- **F-5 (P1) — DÜZELTME GEREKMEDİ:** Canlı DB kanıtı: onaylı metin, testte üstü çizilen iki "T:" satırını İÇERMİYOR — mekanizma doğru çalışmış. Onay sonrası görülen "eski+yeni" metin, çizilmeMİŞ "Dr:" soruları + altlarına yazılan yeni ifadelerdi. Veri kaybı/kaçak yok; F-4 düzeltmesi bu algı karışıklığını da önler (IU ne çizdiğini gönderdikten sonra da görür).
- **F-10 (P2):** Kök neden: mammoth `extractRawText`, Word'ün OTOMATİK liste işaretlerini ("1.", "A)") düşürüyordu → ayrıştırıcı soru sınırı bulamıyordu. `lib/soru/dosyadanGetir.ts` docx yolu HTML çıkarımına geçirildi; liste yapısından işaretler yeniden üretiliyor (`htmldenSatirlar`: 1. seviye "N.", derin seviye "A)"; elle yazılmış işaret ikilenmez). Dosya da artık yapıştırmayla aynı ayrıştırıcıdan aynı kalitede geçer.
- **F-8 (P2):** IU soru sayfasında serbest text alanı tamamen kalktı (`SoruIceAktar` `yalnizDosya` modu) — tek yol: dosya yükleme + yapılandırılmış tablo. NOT: aynı bileşen PM talep formunda (hazır soru seti) da kullanılıyor; orası test kapsamı dışı olduğundan PM tarafında yapıştırma korundu — İskender kararı beklenir.
- **F-9 (P2):** Buton adı "Dosya Yükleyin" oldu; "Excel şablonu indir" linki ve üreten fonksiyon kaldırıldı.
- **F-6 (P3):** `useBunnyIslemeDurumu`'na `onHazir` geri bildirimi eklendi; video sayfası hazıra geçişte kapak+kart medyasını sayaçla yeniden kurup kapak URL'ine önbellek-aşan parametre ekliyor — kapak ve oynatma refresh'siz gelir.
- **F-7 (P3):** Üç listede (senaryolar/videolar/soru-setleri) "Son Durum" başlığına sabit genişlik (`w-44`), rozete `whitespace-nowrap` — başlık kayması bitti.
- **F-1 (P4):** Gönderilmiş talep detayından "Dosya Ekle" yolu tamamen kaldırıldı (yükleme kodu dahil); dosya ekleme yalnız talep açılırken. PM'in dosya silmesi korundu.
- **F-2 (P4):** Talep detayındaki "Senaryolar" butonu kaldırıldı — PM takibi Navbar Senaryolar sekmesi + rozet.
- **F-11 (P5):** Rozetli pillerde (Talepler/Senaryolar/Videolar/Soru Setleri/Yayın Yönetimi/Öneriler-UTT) renk artık iş durumuna bağlı: bekleyen iş varsa bordo, yoksa nötr (aktif sayfa kalın yazıyla belli). Soru seti onaylanınca Soru Setleri söner, Yayın Yönetimi bordo yanar.
- **F-12 (P6):** `app/onaylanan-talepler/page.tsx` — IU'ya özel sekme (Navbar masaüstü + mobil): talep başına onaylı senaryo metni, oynatılabilir video (iframe) ve doğru şıkkı işaretli soru seti, salt-okuma.
- Doğrulama (toplu tek sefer): smoke 6/6, tsc + `npm run denetim` + `npm run lint:mimari` temiz.
- **Fiziksel teyit İskender'in turunda:** (a) IU gönderim sonrası işaretli metni görüyor mu, (b) Word yüklemesi soruları tabloya dağıtıyor mu, (c) video kapak/oynatma refresh'siz geliyor mu, (d) pill renkleri iş akışını izliyor mu, (e) Onaylanan Talepler sayfası.
